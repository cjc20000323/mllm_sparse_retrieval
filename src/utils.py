import argparse

import torch
from tqdm import tqdm


def calculate_recall(run, target):
    '''
    :param run: 搜索结果
    :param k: 计算r@k中的k
    :param target: 真实对应关系，也就是应该找出来的结果
    :return:
    '''
    torch.isin()
    pass
